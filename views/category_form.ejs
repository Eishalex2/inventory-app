<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <%- include('./sidebar') %>

  <form action="" method="POST">
    <div class="form-group">
      <label for="name">Category:</label>
      <input type="text" id="name" name="name" required="true" value=<%= category === false ? '' : category.name %>>
    </div>
    <button type="submit">Submit</button>
  </form>

  <% if (errors) { %>
    <ul>
      <% errors.forEach(error => { %>
        <li><%= error.msg %></li>
      <% }) %>
    </ul>
  <% } %>
  
</body>
</html>